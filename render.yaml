services:
  - type: web
    name: voice-assistant
    env: python
    buildCommand: |
      apt-get update && apt-get install -y python3-sdl2
      pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0 
